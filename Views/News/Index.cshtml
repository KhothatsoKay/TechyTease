@model List<NewsArticle>

<h1>News Articles</h1>

@foreach (var article in Model)
{
    <div>
        <h2>@article.Title</h2>
        <p>@article.Description</p>
        <img src="@article.Image" alt="Article Image" width="200">
        <p><a href="@article.Url">Read More</a></p>
    </div>
}
